# AMO credentials for signing the Firefox build.
# Get them at https://addons.mozilla.org/en-US/developers/addon/api/key/
# Copy to .env (gitignored) and fill in. Never commit the real values.
WEB_EXT_API_KEY=user:00000000:000
WEB_EXT_API_SECRET=
